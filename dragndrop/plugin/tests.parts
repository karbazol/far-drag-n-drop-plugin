Import('*')

env.PartName('tests')

env.DependsOn([
        Component('tut'),
        Component('texts'),
        Component('dll.headers'),
        ])

env.Append(
    CPPPATH=['#/../common_${A_OR_U}', '.', 'src'],
    CXXFLAGS=['/EHsc', '/GR'],
    CPPDEFINES=['_FAR_USE_FARFINDDATA'],
    )

env.UnitTest('configure_test${TARGET_ARCH}', ['tests/configure_test.cpp', 'src/configure.cpp', 'tests/configure_stubs.cpp'], make_pdb = True)

# vim: set et ts=4 sw=4 ft=python :

